apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler

metadata:
  name: driver-service
  namespace: f1-platform

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: driver-service

  minReplicas: 3
  maxReplicas: 6

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60